extends layout

block content
    h1 #{title}:  #{furniture.name}

    p Do you really want to delete this furniture?

    form(method='POST')
      div.form-group
        input#furniture_id.form-control(type='hidden', name='id', value=furniture._id )

      button.btn.btn-primary(type='submit') Delete

    